apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: netsentinel
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
